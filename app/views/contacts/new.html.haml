
.form
  %h2= t('contacts.new.heading')
  
  = semantic_form_for @contact, :url => contacts_path  do |f|
    
    = f.inputs do
      = f.input :email, :as => :string 
      = f.input :subject, :as => :string 
      = f.input :body, :as => :text
      
    = f.buttons do    
      = f.commit_button :send_message

.clear